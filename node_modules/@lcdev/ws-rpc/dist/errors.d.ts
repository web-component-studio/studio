export declare enum ErrorType {
    BuildError = 1,
    Timeout = 2,
    ConnectionNotOpen = 3,
    Response = 4,
    InvalidWebsocket = 5,
    Unknown = -1
}
export declare class RpcError extends Error {
    readonly code: ErrorType;
    constructor(msg: string, code: ErrorType);
}
