import { css } from 'lit';

export default css`
:host {
  display: block;
  padding: var(--studio-spacing-medium);
}

h2 {
  margin: 0 0 var(--studio-spacing-small) 0;
  font-size: 1rem;
}

ul {
  list-style: none;
  margin: 0;
  padding: 0;
}`;
